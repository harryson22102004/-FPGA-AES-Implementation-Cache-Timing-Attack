# FPGA AES Implementation + Cache Timing Attack
FPGA-mapped AES implementation attacked via cache-timing side channels.
### ■ Research Highlights
- **T-Table AES Model**: Simulates the 4-table AES implementation with cache-line granularity access tracking- **Bernstein Attack**: Correlates encryption time distributions against key-byte hypotheses to recover 16 by- **Algorithms Focus**: Demonstrates that constant-time code (bitsliced AES) eliminates timing leakage, motiv**Tech Stack:** Python, `NumPy`.
